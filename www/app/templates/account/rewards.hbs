<div class="alert ">
   <span class="label label-default">Незрелые</span> <span class="label label-success">Подтвержденные</span>
 <div class="alert index_block2 table-responsive">





    {{#if model.rewards}}










  <table class="table table-condensed table-striped">
  <thead style="color:#000; background-color:rgba(0, 0, 0, 0.13);">
      <tr>
    <th>Высота Блока</th>
	<th>Награда</th>
<th>Время решения блока </th>
        
        
        
      </tr>
    </thead>
    <tbody>
      {{#each model.rewards as |tx|}}
        <tr>
	  
          <td> <a href="https://meta.nilu.tech/block/{{tx.blockheight}}" rel="nofollow" target="_blank" style="color:#000">{{format-number  tx.blockheight}}</a> </td>



          <td>
		{{#if tx.immature}}
                        <span class="label label-default">{{format-balance tx.reward}}</span>
                


{{else}}
                        <span class="label label-success">{{format-balance tx.reward}}</span>
                {{/if}}





	  </td>
          <td>{{format-date-locale tx.timestamp}}</td>

        </tr>
      {{/each}}
    </tbody>
  </table>


    
   
  {{else}}
  <h3>No rewards yet</h3>
  {{/if}}
</div></div>
